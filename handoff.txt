Digital CookBook — continuing. Read PLAN.md at the repo root first (source of truth).
Work in baby steps: propose ONE small task, show code, I approve, we test.

STATUS (2026-07-12, wrap-up):
  - Backend → Render: https://digital-cookbook-api.onrender.com
  - Frontend → Cloudflare Worker: https://digital-cookbook.omergrinwald14.workers.dev
  - Push to main auto-deploys both (~2-4 min Render, ~1 min Cloudflare).
  - Server is ALWAYS UP now (user, 2026-07-12) — cold-start gotchas and the
    cold-start/share-matrix backlog items are retired.
  - DONE this session, all deployed + user-verified live:
    * Search also matches tag names (applySearch third OR-condition).
    * PHASE 8 complete — manual recipe entry:
        POST /recipes (RecipeCreate; reuses save_recipe; DDL run: dropped
        NOT NULL on recipes.source_url — unique(owner,source_url) safe,
        NULLs don't collide). "✎ Or create one yourself" form under Add a
        recipe (title + ingredients/steps one-per-line + optional photo).
        POST /recipes/{id}/photo (multipart; NEW DEP python-multipart;
        image/* only, 5MB cap) → existing thumbnails bucket, name
        manual-{id}, public URL into recipes.thumbnail.
    * PHASE 9 complete — sharing (mini social network):
        DDLs run: shared_recipes(recipe_id FK cascade, from_owner,
        to_owner, status pending/accepted/dismissed, unique(recipe_id,
        to_owner)); users(email pk, backfilled the 4 family emails);
        friends(owner, friend_email — BOTH FK→users(email) cascade).
        POST /users = registry upsert, fired by the login form.
        GET/POST/DELETE /friends (POST 404s unregistered emails, 400 self).
        POST /recipes/{id}/share (unknown recipient 404, self 400,
        auto-adds recipient to sender's friends). GET /shared inbox
        (pending only, embeds recipe preview). POST /shared/{id}/accept
        copies via save_recipe (URL-dedupe applies; tag names the
        recipient lacks are skipped) / {id}/dismiss. Re-share revives a
        dismissed offer (upsert).
        UI: Share button on each card → select of friends + "✉ Someone
        else…" prompt (backend 404 text surfaced). "Shared with me"
        section above Tags, hidden when empty; Add/Dismiss rows fade
        (.pending) during the round-trip.
        delete_user also deletes the users row (cascades friends lists).
    * 9-7 share button restyle (eba2873): faint ↗ (was reusing
      .flag-toggle's 40% opacity) → .share-btn accent pill, inline
      Feather share-2 SVG + "Share" label, margin-left:auto pushes it
      to the right edge of .recipe-tools. Picker logic untouched.
      User-verified locally; deployed (live spot-check not reported).

NEXT SESSION (in order):
  1. TASK 7-11 (user will say "task 11"; only after ~2026-07-15):
     a. User runs in Supabase SQL editor:  alter table recipes drop column tag_id;
     b. IMMEDIATELY push the prepared cleanup commit: remove the detach
        lines in storage.py delete_tag (cascade handles it), rewrite
        PLAN.md "Data model" section (tags + recipe_tags + users/friends/
        shared_recipes now), tick 7-11.
     c. Tiny window between (a) and deploy where deleting a tag 500s — fine.
     d. Test: delete a tag that's on recipes → recipes lose only that chip.
  2. Backlog (PLAN.md): 5-3f delete-user button; require X-User; claim
     NULL-owner legacy rows; delete_user shared_recipes-orphan cleanup;
     /import duplicate response tags embed.
  3. (Family Android re-test after login deploy: CONFIRMED working 2026-07-12.)

GOTCHAS (Windows/dev):
  - py NOT python; backend venv at backend/.venv.
  - Local frontend test: py -m http.server 5500 -d frontend (8080 BLOCKED).
    Ctrl+Shift+R after JS/CSS edits AND after the user-switch reload —
    http.server sends no cache headers, so heuristic caching serves stale
    CSS (bit us twice in 9-6). Local frontend talks to the LIVE Render
    backend — wait ~3 min after pushing backend changes before UI tests.
  - THIS MACHINE: Python requests randomly dies with SSLEOFError against
    healthy hosts (supabase.co, onrender.com). Retry; if it persists,
    verify with curl.exe (schannel) before suspecting the server.
  - Supabase SQL editor runs a pasted script as ONE transaction — a failed
    statement rolls back the earlier ones (a create table silently vanished).
  - users/friends store LOWERCASE emails and FKs enforce it — share/friend
    endpoints pass user.lower(); recipes.owner still stores the raw header
    (frontend lowercases at login anyway).
  - recipes/tags contain legacy NULL-owner rows — invisible to the app,
    skipped by the users backfill (backlog: claim them for Omer).
  - CSS: the hidden attribute loses to ANY display: rule — pattern:
    #manual-form[hidden] / #login-screen[hidden] { display:none; }.
  - Scratch scripts: $env:PYTHONPATH=<backend dir> + venv python -X utf8;
    .env loads by absolute path so cwd doesn't matter.
  - Supabase client = module singleton behind @_synchronized RLock — keep.
    RLock means nested storage calls are fine (resolve_share→save_recipe).
  - DDL → Supabase dashboard SQL editor only. DML works via service key.
  - PGRST201 lesson: a new table relating already-related tables can break
    existing embeds → verify live GET /recipes right after ANY DDL
    (9-1's shared_recipes FK was clean; friends→users embeds unused).
  - PostgREST: filtered !inner embeds hide the row's other embedded rows;
    .in_("id", []) errors — short-circuit; embed anti-join = .is_(embed, "null").
  - PowerShell commit messages: no embedded double quotes — args split.
  - Deploy gap: Cloudflare ~1 min, Render ~2-4 min; verify by fetching the
    live asset, not the dashboard build list.
  - Secrets: backend/.env local (gitignored); Render env vars live.
  - LLM is gemini-2.5-flash. Cloudflare strips .html (307 keeps params).
  - SW_VERSION still "v5 cold-start-retry" — sw.js untouched in Phases 8-9
    (it caches NO assets; stale CSS is browser caching, not the SW).
  - Curl/scratch downloads → session scratchpad dir, NOT repo root.
  - Keep handoff.txt uncommitted (tracked; never stage it).
  - Share-to-app UX is SETTLED: Android → share.html → app; iOS → Shortcut.
    That flow only calls /import — Phases 8-9 never touched it. ("Share"
    now ALSO means recipe→friend in-app; don't confuse the two.)
  - User prefers plain-language test instructions (no bare DevTools jargon;
    if a check needs the DB, run it for them via script and report).
